Smartmatch is experimental at Disketo_Instruction_Set.pm line 409.
compute_custom_for_each_dir	meta_name  computer
	Computes given computer on each dir to produce the given meta

compute_custom_for_each_file	meta_name  computer
	Computes given computer on each file to produce the given meta

compute_dirs_matching_custom	matcher
	Computes meta with dirs matching given matcher

compute_dirs_matching_pattern	pattern
	Computes meta with dirs matching given pattern

compute_files_matching_custom	matcher
	Computes meta with files matching given matcher

compute_files_matching_pattern	pattern
	Computes meta with files matching given pattern

compute_files_of_dir_matching_custom	matcher
	Computes meta with files of each dir matching given matcher

compute_files_of_dir_matching_pattern	pattern
	Computes meta with files of each dir matching given pattern

context_stats	
	Prints the informations about the current context

count_files	
	Computes the counts of the files in each dir

filter_dirs_by_meta	meta_name
	Filters dirs matching given meta

filter_dirs_matching_custom	predicate
	Filters dirs matching given predicate

filter_dirs_matching_pattern	pattern
	Filters dirs matching given pattern

filter_dirs_with_files_matching_custom	files_predicate  min_count
	Filters dirs with having at least given number of files matching given predicate

filter_dirs_with_files_matching_meta	files_meta_name  min_count
	Filters dirs with having at least given number of files matching given meta

filter_dirs_with_files_matching_pattern	files_pattern  min_count
	Filters dirs with having at least given number of files matching given pattern

filter_duplicate_dirs_by_custom_comparer	comparer
	Filters duplicate dirs by custom comparer

filter_duplicate_dirs_by_custom_groupper	groupper  meta_name
	Filters duplicate dirs by given groupper

filter_duplicate_dirs_by_name	
	Filters duplicate dirs by name

filter_duplicate_dirs_with_common_files_by_custom_comparer	files_comparer
	Filters duplicate dirs with common files by custom comparer

filter_duplicate_dirs_with_common_files_by_custom_groupper	groupper  meta_name
	Filters duplicate dirs with common files by their custom groupper

filter_duplicate_dirs_with_common_files_by_name	
	Filters duplicate dirs with common files by their name

filter_duplicate_files_by_custom_comparer	comparer
	Filters duplicate files by custom comparer

filter_duplicate_files_by_custom_groupper	groupper  meta_name
	Filters duplicate files by given groupper

filter_duplicate_files_by_name	
	Filters duplicate files by name

filter_files_by_meta	meta_name
	Filters files matching given meta

filter_files_matching_custom	predicate
	Filters files matching given predicate

filter_files_matching_pattern	pattern
	Filters files matching given pattern

group_dirs_by_custom	groupper  meta_name
	Groups dirs by custom groupper

group_dirs_by_names	
	Groups dirs by names

group_files_by_custom	groupper  meta_name
	Groups files by custom groupper

group_files_by_names	
	Groups files by names

load	file_or_dir
	Loads the resources from the filesystem(s)

print_dirs_custom	printer
	Prints dirs by custom printer

print_dirs_simply	
	Prints dirs simply

print_files_custom	printer
	Prints files by custom printer

print_files_simply	
	Prints files simply

That's all folks! at ./run-disketo-script.pl line 32.
