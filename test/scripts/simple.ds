# simple script
filter dirs matching-pattern "(text)|(doc)" case-insensitive
compute for-each-file custom "files matching" by-computer sub { 
	my ($file, $context) = @_;

	return ($file =~ /(\.xlsx?)|(\.docx?)/);
}
filter dirs having-files matching-pattern "(\.xlsx?)|(\.docx?)" case-sensitive more-than-of-them $$
print files simply
