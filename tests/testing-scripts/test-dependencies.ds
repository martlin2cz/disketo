# tests the dependencies

# test the ambigious resolution (little hacky)
# UNCOMMENT TO TEST
# print files with meta "(user specified)" 

# tests the autogenerated metas
filter dirs having none of-the-same name-and-subtree-size

# tests the user specified metas
compute custom-meta by-appling-function sub { 
	my ($file, $context) = @_;
	return length(shift @_);
} to-each-file as-meta "just the file meta"

print files with meta "just the file meta" 

# test the unresolvable meta
print files with meta "hujaja"

